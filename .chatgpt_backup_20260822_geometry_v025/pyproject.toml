[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nd_posture_guard"
version = "0.24.0"
description = "Incremental personal shoulder-posture monitor with persistent training data, Ubuntu dock integration, and robust warning playback."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "PySide6>=6.8,<7",
    "opencv-python>=5.0.0.93,<6",
    "PyYAML>=6.0,<7",
    "numpy>=1.26,<3",
]

[project.scripts]
nd-posture-guard = "nd_posture_guard.main:Main.run"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
